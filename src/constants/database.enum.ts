export enum DATABASE_TABLES {
  USERS = 'users',
  ROLES = 'roles',
  BROKER = 'broker',
  INVOICES = 'invoices',
  LOADS = 'load',
  COMMUNICATIONS = 'communication',
  DOCUEMENT = 'document',
  DRIVER = 'driver',
  DRIVER_SCHEDULE = 'driver_schedule',
  LOAD_ASSIGNMENTS = 'load_assignment'
}

export enum DATABASE_FK {
  FK_MANUFACTURER_ROLE = 'fk_manufacturer_role',

}
